---
title: 6.13 新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators
description: 可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators
volume: 卷二：Web 标准与语言规范
chapter: 第 6 章 JavaScript 语言精要
section: '6.13'
status: 已完成
difficulty: 基础
lastUpdated: '2026-06-26'
tags:
  - 可选链
  - 空值合并
  - Decorators
  - 新语法
keywords:
  - 可选链
  - 空值合并
  - Decorators
  - 新语法
  - 新语法特性
  - 逻辑赋值
  - Pipeline
  - Operator
summary: >-
  本文围绕新语法特性：可选链、空值合并、逻辑赋值、Pipeline
  Operator、Decorators展开，重点说明可选链、空值合并、逻辑赋值、Pipeline
  Operator、Decorators，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
learningGoals:
  - 理解新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators的核心概念、问题背景和适用边界。
  - 掌握新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators在真实前端工程中的典型用法和验证方式。
  - 能够识别新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators相关的常见误区，并选择合适的优化或治理策略。
prerequisites: []
related:
  - title: 6.1 JS 历史与标准：ES1-6 里程碑、TC39 流程、ES 年度版本、Babel 转译原理
    slug: volume-02/chapter-06/6.1-js-history
  - title: 6.2 类型系统：原始类型 vs 对象类型、typeof/instanceof、隐式转换、Symbol、BigInt
    slug: volume-02/chapter-06/6.2-type-system
crossRefs: []
references:
  - title: TC39 finished proposals
    url: 'https://github.com/tc39/proposals/blob/main/finished-proposals.md'
  - title: TC39 process document
    url: 'https://tc39.es/process-document/'
  - title: ECMAScript specification draft
    url: 'https://tc39.es/ecma262/'
  - title: MDN JavaScript Guide
    url: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide'
---
## 速览

- 本文围绕新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators展开，重点说明可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
- 阅读重点是把「新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators」放回 第 6 章 JavaScript 语言精要 的知识链路中理解，而不是只记忆孤立定义。
- 工程实践中需要同时关注 可选链、空值合并、Decorators、新语法 等关键因素，并用可观察的指标或测试结果验证判断。
- 如果你已经熟悉前置概念，可以优先阅读「工作原理」「工程实践」和「常见误区」三部分。

## 背景与问题

JavaScript 语法持续演进，TC39 每年引入的新特性旨在提升代码安全性、可读性和表达能力。本节聚焦近年来对工程实践影响最深远的语法特性。

### 可选链（Optional Chaining，ES2020）

可选链操作符 `?.` 允许安全地访问嵌套属性，当引用为 `null` 或 `undefined` 时短路返回 `undefined`，而非抛出 `TypeError`。

```js
const street = user?.address?.street;
// 等价于：user == null ? undefined : user.address == null ? undefined : user.address.street
```

## 核心概念

| 概念 | 说明 |
|---|---|
| 可选链 | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| 空值合并 | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| Decorators | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| 新语法 | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |

这些概念之间往往存在依赖关系：先理解术语边界，再观察运行机制，最后才能做出稳定的工程决策。

## 工作原理

可选链操作符 `?.` 允许安全地访问嵌套属性，当引用为 `null` 或 `undefined` 时短路返回 `undefined`，而非抛出 `TypeError`。

```js
const street = user?.address?.street;
// 等价于：user == null ? undefined : user.address == null ? undefined : user.address.street
```

可选链支持三种使用形式：

- `obj?.prop`：可选属性访问。
- `obj?.[expr]`：可选动态属性访问。
- `func?.(...args)`：可选函数调用。

<Callout type="warning" title="可选链不能用于赋值左侧">
`user?.name = 'Tom'` 是语法错误。可选链只能用于读取，不能用于赋值。
</Callout>

### 空值合并（Nullish Coalescing，ES2020）

空值合并操作符 `??` 仅在左侧为 `null` 或 `undefined` 时返回右侧值，与 `||` 不同，它不会将 `0`、`""`、`false` 视为 falsy 而跳过。

```js
const value = input ?? 'default'; // input 为 null/undefined 时才使用默认值
const count = response.count ?? 10; // count 为 0 时仍使用 0，不会回退到 10
```

`??` 与 `||` 不能混用而不加括号，因为它们的优先级和结合性可能导致意外行为：

```js
const x = 0 || 1 ?? 2; // SyntaxError
const y = (0 || 1) ?? 2; // OK
```

### 逻辑赋值（Logical Assignment，ES2021）

逻辑赋值运算符将逻辑运算与赋值结合：

- `a ||= b`：等价于 `a || (a = b)`，仅当 `a` 为 falsy 时赋值。
- `a &&= b`：等价于 `a && (a = b)`，仅当 `a` 为 truthy 时赋值。
- `a ??= b`：等价于 `a ?? (a = b)`，仅当 `a` 为 nullish 时赋值。

```js
// 初始化默认值
config.timeout ??= 5000;

// 条件性启用
feature.enabled &&= user.hasPermission;
```

注意：逻辑赋值的右侧仅在条件满足时求值，具有短路特性。

### Pipeline Operator（提案中）

Pipeline Operator `|>` 允许将表达式的结果作为参数传递给下一个函数，改善函数调用的可读性：

```js
// 注意：Pipeline Operator 仍在提案中，示例只表达意图，不代表最终标准语法。
const result = format(add(double(value), 10));
```

目前 TC39 对具体语法和占位符模型仍有讨论，生产环境不应依赖非标准 Pipeline 语法。可使用清晰的中间变量、函数组合工具，或等待 Babel/TypeScript 与运行时对最终标准的支持。

### Decorators（Stage 3）

Decorators（装饰器）是一种用于修改类和类成员的声明式语法。TC39 的 Decorators 提案经历了多次重大修订，截至 2026 年 6 月仍不是 finished proposal，工程使用时必须明确是 TypeScript legacy decorators、Babel legacy decorators，还是 TC39 Stage 3 decorators。

```js
function logged(value, { kind, name }) {
  if (kind === 'method') {
    return function(...args) {
      console.log(`Calling ${name}`);
      return value.apply(this, args);
    };
  }
}

class Example {
  @logged
  sum(a, b) {
    return a + b;
  }
}
```

装饰器函数接收被装饰的值和上下文对象（包含 `kind`、`name`、`access`、`static`、`private` 等元数据），可返回新的值替换原值。

Babel 的 `@babel/plugin-proposal-decorators` 可以选择不同版本的装饰器语义；TypeScript 的 `experimentalDecorators` 长期服务于 legacy decorators。新项目应在文档中写明装饰器版本，避免在框架、编译器和运行时之间混用不兼容语义。

### ES2025/ES2026 进展

ES2025 已包含同步 Iterator helpers、Set 集合方法、Import Attributes/JSON Modules、RegExp.escape、RegExp modifiers、重复命名捕获组、Promise.try、Float16Array 等特性。它们多数是 API 或模块能力，不全是新语法，但会显著影响集合处理、正则安全和模块加载方式。

TC39 finished proposals 中还列出了预计 ES2026/ES2027 发布的特性，例如 Array.fromAsync、Error.isError、Uint8Array Base64、JSON.parse source text access、Upsert、Temporal 和 Explicit Resource Management。项目中不要把 Stage 3 或 finished-but-not-yet-shipped 的能力直接当作所有运行时都可用，应通过 Baseline、MDN、Can I Use、Node 版本和目标浏览器矩阵确认。

可选链支持三种使用形式：

- `obj?.prop`：可选属性访问。
- `obj?.[expr]`：可选动态属性访问。
- `func?.(...args)`：可选函数调用。

<Callout type="warning" title="可选链不能用于赋值左侧">
`user?.name = 'Tom'` 是语法错误。可选链只能用于读取，不能用于赋值。
</Callout>

### 空值合并（Nullish Coalescing，ES2020）

空值合并操作符 `??` 仅在左侧为 `null` 或 `undefined` 时返回右侧值，与 `||` 不同，它不会将 `0`、`""`、`false` 视为 falsy 而跳过。

```js
const value = input ?? 'default'; // input 为 null/undefined 时才使用默认值
const count = response.count ?? 10; // count 为 0 时仍使用 0，不会回退到 10
```

`??` 与 `||` 不能混用而不加括号，因为它们的优先级和结合性可能导致意外行为：

```js
const x = 0 || 1 ?? 2; // SyntaxError
const y = (0 || 1) ?? 2; // OK
```

### 逻辑赋值（Logical Assignment，ES2021）

逻辑赋值运算符将逻辑运算与赋值结合：

- `a ||= b`：等价于 `a || (a = b)`，仅当 `a` 为 falsy 时赋值。
- `a &&= b`：等价于 `a && (a = b)`，仅当 `a` 为 truthy 时赋值。
- `a ??= b`：等价于 `a ?? (a = b)`，仅当 `a` 为 nullish 时赋值。

```js
// 初始化默认值
config.timeout ??= 5000;

// 条件性启用
feature.enabled &&= user.hasPermission;
```

注意：逻辑赋值的右侧仅在条件满足时求值，具有短路特性。

### Pipeline Operator（提案中）

Pipeline Operator `|>` 允许将表达式的结果作为参数传递给下一个函数，改善函数调用的可读性：

```js
// 注意：Pipeline Operator 仍在提案中，示例只表达意图，不代表最终标准语法。
const result = format(add(double(value), 10));
```

目前 TC39 对具体语法和占位符模型仍有讨论，生产环境不应依赖非标准 Pipeline 语法。可使用清晰的中间变量、函数组合工具，或等待 Babel/TypeScript 与运行时对最终标准的支持。

### Decorators（Stage 3）

Decorators（装饰器）是一种用于修改类和类成员的声明式语法。TC39 的 Decorators 提案经历了多次重大修订，截至 2026 年 6 月仍不是 finished proposal，工程使用时必须明确是 TypeScript legacy decorators、Babel legacy decorators，还是 TC39 Stage 3 decorators。

```js
function logged(value, { kind, name }) {
  if (kind === 'method') {
    return function(...args) {
      console.log(`Calling ${name}`);
      return value.apply(this, args);
    };
  }
}

class Example {
  @logged
  sum(a, b) {
    return a + b;
  }
}
```

装饰器函数接收被装饰的值和上下文对象（包含 `kind`、`name`、`access`、`static`、`private` 等元数据），可返回新的值替换原值。

Babel 的 `@babel/plugin-proposal-decorators` 可以选择不同版本的装饰器语义；TypeScript 的 `experimentalDecorators` 长期服务于 legacy decorators。新项目应在文档中写明装饰器版本，避免在框架、编译器和运行时之间混用不兼容语义。

### ES2025/ES2026 进展

ES2025 已包含同步 Iterator helpers、Set 集合方法、Import Attributes/JSON Modules、RegExp.escape、RegExp modifiers、重复命名捕获组、Promise.try、Float16Array 等特性。它们多数是 API 或模块能力，不全是新语法，但会显著影响集合处理、正则安全和模块加载方式。

TC39 finished proposals 中还列出了预计 ES2026/ES2027 发布的特性，例如 Array.fromAsync、Error.isError、Uint8Array Base64、JSON.parse source text access、Upsert、Temporal 和 Explicit Resource Management。项目中不要把 Stage 3 或 finished-but-not-yet-shipped 的能力直接当作所有运行时都可用，应通过 Baseline、MDN、Can I Use、Node 版本和目标浏览器矩阵确认。

## 代码示例

```js
// 可选链与空值合并的组合
const userCity = user?.address?.city ?? 'Unknown';

// 安全访问深层配置
const dbPort = config?.databases?.primary?.port ?? 5432;

// 逻辑赋值简化事件处理器注册
class Emitter {
  listeners ??= new Map();
  on(event, handler) {
    this.listeners[event] ??= [];
    this.listeners[event].push(handler);
  }
}

// 可选链调用回调
onSuccess?.(result);
```

## 工程实践

### 迁移旧代码到现代语法

```js
// 旧代码
const street = user && user.address && user.address.street;
const name = user.name || 'Anonymous';

// 现代代码
const street = user?.address?.street;
const name = user.name ?? 'Anonymous';
```

### TypeScript 中的精确类型收窄

可选链和空值合并配合 TypeScript 的类型守卫可实现更精确的类型推断：

```ts
function process(data: { items?: string[] }) {
  // data.items 被推断为 string[] | undefined
  const first = data.items?.[0];
  // first 被推断为 string | undefined
  return first ?? 'default';
}
```

### Babel 与浏览器兼容性

ES2020 的可选链和空值合并已被所有现代浏览器支持。若需兼容旧版浏览器，Babel 转译后的代码会引入辅助函数：

```js
// 转译后（简化示意）
var _user$address;
var street = (_user$address = user === null || user === void 0 ? void 0 : user.address) === null || _user$address === void 0 ? void 0 : _user$address.street;
```

对于大量嵌套可选链的代码，转译后体积膨胀明显，应评估目标环境是否确实需要转译。

在真实项目中落地新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

在真实项目中落地新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

## 常见误区

| 陷阱 | 现象 | 解决方案 |
|------|------|---------|
| `??` 与 `||` 混用不加括号 | 语法错误或意外优先级 | 混用时始终显式加括号 |
| 可选链用于赋值左侧 | 语法错误 | 使用条件判断包裹赋值逻辑 |
| `obj?.prop` 整体为 undefined 时的运算 | `obj?.prop + 1` 得到 NaN | 配合 `??` 提供默认值：`(obj?.prop ?? 0) + 1` |
| 装饰器版本不兼容 | Babel Legacy 与 TC39 新提案语法不同 | 明确项目使用的装饰器版本，不混用 |
| 将 Stage 3 或 finished proposal 当作已全量可用 | 本地或新浏览器可用，旧环境/Node 版本失败 | 通过兼容矩阵、转译配置和运行时检测确认 |
| `??=` 与 `=` 的语义混淆 | `a ??= b` 仅在 nullish 时赋值，而 `a = b` 总是赋值 | 根据业务意图选择正确的赋值方式 |

## 对比总结

| 维度 | 关注点 | 判断标准 |
|---|---|---|
| 概念理解 | 新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators的定义、边界和相邻概念。 | 能用自己的话解释，并指出至少一个反例。 |
| 工程使用 | 代码、配置、流程或团队规范中的落地方式。 | 能在真实场景中给出可执行步骤。 |
| 风险控制 | 性能、兼容、安全、可维护性和协作成本。 | 有测试、监控、回滚或审查机制支撑。 |
| 长期维护 | 文档、自动化检查和知识传递。 | 后续成员能按同一标准继续演进。 |

## 自测题

<Quiz
  question={"学习新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators时，最能体现工程化理解的是哪一项？"}
  options={[
    "只记住一个简短定义",
    "能说明适用场景、限制条件、验证方式和常见风险",
    "只复制文章中的代码片段",
    "只关注工具或 API 名称"
  ]}
  correctIndex={1}
  explanation={"工程化理解不止停留在概念定义，还要能说明适用边界、验证方式和风险控制。这样才能把新语法特性：可选链、空值合并、逻辑赋值、Pipeline Operator、Decorators从知识点转化为可落地的工程判断。"}
/>

## 参考资料

- [TC39 finished proposals](https://github.com/tc39/proposals/blob/main/finished-proposals.md)
- [TC39 process document](https://tc39.es/process-document/)
- [ECMAScript specification draft](https://tc39.es/ecma262/)
- [MDN JavaScript Guide](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide)
