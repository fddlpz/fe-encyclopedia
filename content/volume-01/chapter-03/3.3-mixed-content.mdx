---
title: 3.3 混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求
description: 主动与被动混合内容、升级不安全请求
volume: 卷一：计算机科学与网络基础
chapter: 第 3 章 Web 安全基础
section: '3.3'
status: 已完成
difficulty: 基础
lastUpdated: '2026-06-26'
tags:
  - Mixed Content
  - HTTPS
  - 安全
keywords:
  - Mixed Content
  - HTTPS
  - 安全
  - 混合内容
  - Mixed
  - Content
  - 主动与被动混合内容
  - 升级不安全请求
summary: >-
  本文围绕混合内容（Mixed
  Content）：主动与被动混合内容、升级不安全请求展开，重点说明主动与被动混合内容、升级不安全请求，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
learningGoals:
  - 理解混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求的核心概念、问题背景和适用边界。
  - 掌握混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求在真实前端工程中的典型用法和验证方式。
  - 能够识别混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求相关的常见误区，并选择合适的优化或治理策略。
prerequisites: []
related:
  - title: 3.2 内容安全策略（CSP）：指令集、report-uri、nonce / hash、CSP Level 3
    slug: volume-01/chapter-03/3.2-csp
  - title: >-
      3.4
      安全头部：HSTS、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、Permissions-Policy、COOP/COEP
    slug: volume-01/chapter-03/3.4-security-headers
  - title: 3.1 同源策略（SOP）：源的定义、限制范围、浏览器的沙箱机制
    slug: volume-01/chapter-03/3.1-sop
crossRefs:
  - title: 3.2 内容安全策略（CSP）：指令集、report-uri、nonce / hash、CSP Level 3
    slug: volume-01/chapter-03/3.2-csp
  - title: >-
      3.4
      安全头部：HSTS、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、Permissions-Policy、COOP/COEP
    slug: volume-01/chapter-03/3.4-security-headers
references:
  - title: 'W3C: Mixed Content'
    url: 'https://www.w3.org/TR/mixed-content/'
  - title: 'W3C: Upgrade Insecure Requests'
    url: 'https://www.w3.org/TR/upgrade-insecure-requests/'
  - title: 'MDN: Mixed content'
    url: >-
      https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Mixed_content
  - title: 'RFC 6797: HTTP Strict Transport Security'
    url: 'https://datatracker.ietf.org/doc/html/rfc6797'
  - title: MDN HTTP
    url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP'
  - title: IETF RFC Index
    url: 'https://www.rfc-editor.org/rfc-index.html'
---
## 速览

- 本文围绕混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求展开，重点说明主动与被动混合内容、升级不安全请求，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
- 阅读重点是把「混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求」放回 第 3 章 Web 安全基础 的知识链路中理解，而不是只记忆孤立定义。
- 工程实践中需要同时关注 Mixed Content、HTTPS、安全 等关键因素，并用可观察的指标或测试结果验证判断。
- 如果你已经熟悉前置概念，可以优先阅读「工作原理」「工程实践」和「常见误区」三部分。

## 背景与问题

混合内容是指 HTTPS 页面加载 HTTP 资源。HTTPS 保护了主文档，但 HTTP 子资源可能被中间人读取或篡改，因此浏览器会自动升级一部分请求并阻断另一部分请求。

旧资料常把混合内容分成“主动内容”和“被动内容”。现代规范更强调两类：

| 类型 | 浏览器处理 | 示例 |
|---|---|---|
| 可升级内容 | 浏览器尝试把 `http://` 自动升级为 `https://` | 部分图片、音频、视频资源 |
| 可阻断内容 | 浏览器直接阻断或在策略下阻断 | 脚本、样式、iframe、fetch、XHR、字体 |

## 核心概念

| 概念 | 说明 |
|---|---|
| Mixed Content | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| HTTPS | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| 安全 | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |

这些概念之间往往存在依赖关系：先理解术语边界，再观察运行机制，最后才能做出稳定的工程决策。

## 工作原理

| 类型 | 浏览器处理 | 示例 |
|---|---|---|
| 可升级内容 | 浏览器尝试把 `http://` 自动升级为 `https://` | 部分图片、音频、视频资源 |
| 可阻断内容 | 浏览器直接阻断或在策略下阻断 | 脚本、样式、iframe、fetch、XHR、字体 |

脚本和样式属于高风险资源，因为一旦被篡改就能执行任意逻辑或改变页面行为；图片虽然通常风险低一些，但仍可能泄露访问路径、破坏隐私或造成内容欺骗。

<Callout type="warning" title="自动升级不是迁移方案">
浏览器自动升级或 CSP 的 `upgrade-insecure-requests` 只能帮你尝试改写 URL。如果 HTTPS 资源不存在，请求仍会失败。长期方案是清理所有明文资源来源。
</Callout>

脚本和样式属于高风险资源，因为一旦被篡改就能执行任意逻辑或改变页面行为；图片虽然通常风险低一些，但仍可能泄露访问路径、破坏隐私或造成内容欺骗。

<Callout type="warning" title="自动升级不是迁移方案">
浏览器自动升级或 CSP 的 `upgrade-insecure-requests` 只能帮你尝试改写 URL。如果 HTTPS 资源不存在，请求仍会失败。长期方案是清理所有明文资源来源。
</Callout>

## 代码示例

混合内容最常在 HTTPS 改造、CDN 迁移、富文本内容、第三方 SDK 和历史模板里出现。

```html
<!-- HTTPS 页面中的不安全脚本会被阻断。 -->
<script src="http://cdn.example.com/app.js"></script>

<!-- 图片可能被自动升级，但不应依赖浏览器兜底。 -->
<img src="http://static.example.com/banner.jpg" alt="活动图" />
```

可以使用 CSP 指令批量升级资源请求：

```http
Content-Security-Policy: upgrade-insecure-requests
```

也可以配合报告模式发现遗留 HTTP 资源：

```http
Content-Security-Policy-Report-Only:
  default-src https:;
  upgrade-insecure-requests;
  report-uri /csp-report
```

构建阶段可以加入静态检查，先阻止模板和静态 HTML 中继续出现明文资源。

```ts
const insecureResourcePattern = /\b(?:src|href)=["']http:\/\//i

export function assertNoInsecureResource(html: string) {
  // 中文注释：阻止 HTTPS 页面继续引入明文 HTTP 子资源。
  if (insecureResourcePattern.test(html)) {
    throw new Error("发现 http:// 子资源，请改为 https://")
  }
}
```

## 工程实践

HTTPS 全站改造建议按链路推进：

1. 先完成主站、API、静态资源、图片服务、上传域名的 HTTPS 支持。
2. 扫描源码、CMS 模板、富文本、数据库历史内容和第三方配置。
3. 对可控资源统一改成 `https://`，不要只依赖协议相对 URL。
4. 对不可控第三方资源，要求供应商提供 HTTPS，或移除该依赖。
5. 上线 CSP Report-Only，观察真实用户中的遗漏请求。
6. 全量稳定后再考虑 HSTS，避免用户被强制访问尚未支持 HTTPS 的子域名。

常见排查入口：

| 工具 | 用途 |
|---|---|
| DevTools Console | 查看 Mixed Content 警告 |
| DevTools Network | 过滤 `http://` 请求和 blocked 状态 |
| CSP 报告接口 | 收集线上遗留资源 |
| 构建扫描 | 阻止新增明文资源 |
| 反向代理日志 | 查历史 HTTP 回源和重定向 |

如果图片来自用户输入的富文本，保存时就应归一化 URL，而不是渲染时临时替换。

```ts
export function normalizeImageUrl(url: string) {
  const parsed = new URL(url)

  // 中文注释：只允许 HTTPS 图片进入富文本内容，避免渲染 HTTPS 页面时产生混合内容。
  if (parsed.protocol !== "https:") {
    throw new Error("图片地址必须使用 HTTPS")
  }

  return parsed.toString()
}
```

在真实项目中落地混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

在真实项目中落地混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

## 常见误区

| 常见误区 | 风险 | 建议 |
|---|---|---|
| 只查源码，不查数据库内容 | 富文本和历史配置仍可能输出 HTTP 资源 | 扫描实际渲染 HTML 和线上报告 |
| 认为图片混合内容无害 | 图片可泄露路径、被替换、影响可信展示 | 统一迁移到 HTTPS |
| 依赖 `upgrade-insecure-requests` 兜底 | HTTPS 资源不存在时仍失败 | 修复资源源头 |
| 过早开启 HSTS includeSubDomains | 子域名未完成 HTTPS 会被强制升级导致不可访问 | 先灰度短 `max-age` |
| 忽略第三方 SDK | 第三方脚本可能继续拉取 HTTP 子资源 | 建立第三方资源准入清单 |

## 对比总结

| 维度 | 关注点 | 判断标准 |
|---|---|---|
| 概念理解 | 混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求的定义、边界和相邻概念。 | 能用自己的话解释，并指出至少一个反例。 |
| 工程使用 | 代码、配置、流程或团队规范中的落地方式。 | 能在真实场景中给出可执行步骤。 |
| 风险控制 | 性能、兼容、安全、可维护性和协作成本。 | 有测试、监控、回滚或审查机制支撑。 |
| 长期维护 | 文档、自动化检查和知识传递。 | 后续成员能按同一标准继续演进。 |

## 自测题

<Quiz
  question={"学习混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求时，最能体现工程化理解的是哪一项？"}
  options={[
    "只记住一个简短定义",
    "能说明适用场景、限制条件、验证方式和常见风险",
    "只复制文章中的代码片段",
    "只关注工具或 API 名称"
  ]}
  correctIndex={1}
  explanation={"工程化理解不止停留在概念定义，还要能说明适用边界、验证方式和风险控制。这样才能把混合内容（Mixed Content）：主动与被动混合内容、升级不安全请求从知识点转化为可落地的工程判断。"}
/>

## 参考资料

- [W3C: Mixed Content](https://www.w3.org/TR/mixed-content/)
- [W3C: Upgrade Insecure Requests](https://www.w3.org/TR/upgrade-insecure-requests/)
- [MDN: Mixed content](https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Mixed_content)
- [RFC 6797: HTTP Strict Transport Security](https://datatracker.ietf.org/doc/html/rfc6797)
- [MDN HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)
- [IETF RFC Index](https://www.rfc-editor.org/rfc-index.html)
