---
title: 8.1 浏览器进程模型：多进程架构、站点隔离、浏览器内核对比
description: 多进程架构、站点隔离、浏览器内核对比
volume: 卷三：浏览器与运行时
chapter: 第 8 章 浏览器架构与渲染引擎
section: '8.1'
status: 已完成
difficulty: 进阶
lastUpdated: '2026-06-26'
tags:
  - 浏览器
  - 多进程
  - Blink
  - WebKit
keywords:
  - 浏览器
  - 多进程
  - Blink
  - WebKit
  - 浏览器进程模型
  - 多进程架构
  - 站点隔离
  - 浏览器内核对比
summary: >-
  本文围绕浏览器进程模型：多进程架构、站点隔离、浏览器内核对比展开，重点说明多进程架构、站点隔离、浏览器内核对比，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
learningGoals:
  - 理解浏览器进程模型：多进程架构、站点隔离、浏览器内核对比的核心概念、问题背景和适用边界。
  - 掌握浏览器进程模型：多进程架构、站点隔离、浏览器内核对比在真实前端工程中的典型用法和验证方式。
  - 能够识别浏览器进程模型：多进程架构、站点隔离、浏览器内核对比相关的常见误区，并选择合适的优化或治理策略。
prerequisites: []
related:
  - title: 8.2 导航流程：URL 输入到页面显示的全过程
    slug: volume-03/chapter-08/8.2-navigation
  - title: 8.3 渲染管线深入：解析 HTML、样式计算、布局、绘制、合成
    slug: volume-03/chapter-08/8.3-rendering-pipeline
crossRefs:
  - title: 7.9 类型体操与模式：HKT、类型级编程挑战、类型驱动开发（TDD）
    slug: volume-02/chapter-07/7.9-type-gymnastics
  - title: 8.2 导航流程：URL 输入到页面显示的全过程
    slug: volume-03/chapter-08/8.2-navigation
references:
  - title: 'Chrome Developers: Inside look at modern web browser, part 1'
    url: 'https://developer.chrome.com/blog/inside-browser-part1'
  - title: 'Chromium: Site Isolation'
    url: 'https://www.chromium.org/developers/design-documents/site-isolation/'
  - title: 'WebKit Documentation: WebKit2'
    url: 'https://docs.webkit.org/Deep%20Dive/Architecture/WebKit2.html'
  - title: Vite Guide
    url: 'https://vite.dev/guide/'
  - title: Webpack Concepts
    url: 'https://webpack.js.org/concepts/'
  - title: MDN JavaScript Typed Arrays
    url: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Typed_arrays'
---
## 速览

- 本文围绕浏览器进程模型：多进程架构、站点隔离、浏览器内核对比展开，重点说明多进程架构、站点隔离、浏览器内核对比，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
- 阅读重点是把「浏览器进程模型：多进程架构、站点隔离、浏览器内核对比」放回 第 8 章 浏览器架构与渲染引擎 的知识链路中理解，而不是只记忆孤立定义。
- 工程实践中需要同时关注 浏览器、多进程、Blink、WebKit 等关键因素，并用可观察的指标或测试结果验证判断。
- 如果你已经熟悉前置概念，可以优先阅读「工作原理」「工程实践」和「常见误区」三部分。

## 背景与问题

现代浏览器更像一个小型操作系统：浏览器进程负责 UI、权限、网络调度和进程管理；渲染进程运行网页；GPU 进程负责图形相关工作；网络、存储、扩展、音视频等能力也可能拆到独立进程。具体拆分会随浏览器、平台和安全策略变化，但方向一致：用进程隔离提升稳定性和安全性。

在前端工程中，浏览器进程模型：多进程架构、站点隔离、浏览器内核对比通常不是单独存在的知识点，而是连接语言特性、浏览器能力、框架抽象和工程约束的判断入口。理解它的背景，可以帮助我们回答三个问题：为什么需要这个机制、它解决了什么边界问题，以及在什么场景下不应该过度使用。

## 核心概念

| 概念 | 说明 |
|---|---|
| 浏览器 | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| 多进程 | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| Blink | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| WebKit | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |

这些概念之间往往存在依赖关系：先理解术语边界，再观察运行机制，最后才能做出稳定的工程决策。

## 工作原理

| 进程/线程 | 典型职责 |
|---|---|
| Browser Process | 地址栏、标签页、权限、下载、进程调度 |
| Renderer Process | HTML/CSS/JS、DOM、样式、布局、部分绘制 |
| GPU Process | GPU 命令、合成、部分光栅化 |
| Network Service | DNS、连接、缓存、证书、请求调度 |
| Compositor Thread | 合成层调度、滚动和部分动画 |

站点隔离把不同 site 放到不同渲染进程中，降低 renderer 漏洞跨站读取敏感数据的风险。代价是更多进程和内存开销。Chrome/Blink、Safari/WebKit、Firefox/Gecko 都使用多进程架构，但进程模型、站点隔离粒度和平台集成方式不同。

| 进程/线程 | 典型职责 |
|---|---|
| Browser Process | 地址栏、标签页、权限、下载、进程调度 |
| Renderer Process | HTML/CSS/JS、DOM、样式、布局、部分绘制 |
| GPU Process | GPU 命令、合成、部分光栅化 |
| Network Service | DNS、连接、缓存、证书、请求调度 |
| Compositor Thread | 合成层调度、滚动和部分动画 |

站点隔离把不同 site 放到不同渲染进程中，降低 renderer 漏洞跨站读取敏感数据的风险。代价是更多进程和内存开销。Chrome/Blink、Safari/WebKit、Firefox/Gecko 都使用多进程架构，但进程模型、站点隔离粒度和平台集成方式不同。

## 代码示例

前端工程师通常不直接控制进程模型，但需要理解它对性能和安全的影响：

```ts
function logNavigationContext() {
  const navigation = performance.getEntriesByType("navigation")[0] as PerformanceNavigationTiming | undefined

  if (!navigation) {
    return
  }

  // 中文注释：导航计时只能反映页面视角，不能直接告诉你进程分配情况。
  console.table({
    dns: navigation.domainLookupEnd - navigation.domainLookupStart,
    connect: navigation.connectEnd - navigation.connectStart,
    response: navigation.responseEnd - navigation.responseStart,
    domInteractive: navigation.domInteractive,
  })
}
```

排查多进程相关问题时，优先使用浏览器工具：

- Chrome Task Manager：观察标签页、扩展、GPU、网络服务的内存和 CPU。
- Performance 面板：看主线程、合成线程、GPU 相关事件。
- Memory 面板：定位 JS 堆、DOM 节点和 detached DOM。
- `chrome://process-internals`：观察站点实例和进程分配，适合调试隔离行为。

## 工程实践

理解进程模型后，很多现象会更容易定位：

1. 一个标签页崩溃但浏览器不崩溃，是进程隔离带来的稳定性收益。
2. 跨站 iframe、COOP/COEP、SharedArrayBuffer、站点隔离会影响进程和安全边界。
3. GPU 进程异常可能表现为黑屏、闪烁、WebGL 失败或合成回退。
4. 同一个页面内主线程长任务仍会阻塞交互，因为 JS、样式和布局主要在 renderer 内竞争。

```ts
const observer = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    // 中文注释：长任务说明 renderer 主线程被占用，和多进程隔离不是同一层问题。
    console.log("long task", entry.duration)
  }
})

observer.observe({ type: "longtask", buffered: true })
```

在真实项目中落地浏览器进程模型：多进程架构、站点隔离、浏览器内核对比时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

在真实项目中落地浏览器进程模型：多进程架构、站点隔离、浏览器内核对比时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

## 常见误区

| 常见问题 | 处理建议 |
|---|---|
| 把所有卡顿归因于 JS 引擎 | 同时检查样式、布局、绘制、合成和 GPU |
| 认为多进程必然更快 | 多进程主要提升稳定性和安全性，也会增加内存 |
| 忽略 iframe 的隔离成本 | 跨站 iframe 会增加进程和通信成本 |
| 混淆内核和 JS 引擎 | Blink/WebKit/Gecko 是渲染引擎，V8/JavaScriptCore/SpiderMonkey 是 JS 引擎 |
| 只在 Chrome 验证 | Safari/WebKit 和 Firefox/Gecko 仍需单独验证 |

## 对比总结

| 维度 | 关注点 | 判断标准 |
|---|---|---|
| 概念理解 | 浏览器进程模型：多进程架构、站点隔离、浏览器内核对比的定义、边界和相邻概念。 | 能用自己的话解释，并指出至少一个反例。 |
| 工程使用 | 代码、配置、流程或团队规范中的落地方式。 | 能在真实场景中给出可执行步骤。 |
| 风险控制 | 性能、兼容、安全、可维护性和协作成本。 | 有测试、监控、回滚或审查机制支撑。 |
| 长期维护 | 文档、自动化检查和知识传递。 | 后续成员能按同一标准继续演进。 |

## 自测题

<Quiz
  question={"学习浏览器进程模型：多进程架构、站点隔离、浏览器内核对比时，最能体现工程化理解的是哪一项？"}
  options={[
    "只记住一个简短定义",
    "能说明适用场景、限制条件、验证方式和常见风险",
    "只复制文章中的代码片段",
    "只关注工具或 API 名称"
  ]}
  correctIndex={1}
  explanation={"工程化理解不止停留在概念定义，还要能说明适用边界、验证方式和风险控制。这样才能把浏览器进程模型：多进程架构、站点隔离、浏览器内核对比从知识点转化为可落地的工程判断。"}
/>

## 参考资料

- [Chrome Developers: Inside look at modern web browser, part 1](https://developer.chrome.com/blog/inside-browser-part1)
- [Chromium: Site Isolation](https://www.chromium.org/developers/design-documents/site-isolation/)
- [WebKit Documentation: WebKit2](https://docs.webkit.org/Deep%20Dive/Architecture/WebKit2.html)
- [Vite Guide](https://vite.dev/guide/)
- [Webpack Concepts](https://webpack.js.org/concepts/)
- [MDN JavaScript Typed Arrays](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Typed_arrays)
