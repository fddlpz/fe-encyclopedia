---
title: 8.4 光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync
description: 软件光栅化 vs GPU 光栅化、Skia、VSync
volume: 卷三：浏览器与运行时
chapter: 第 8 章 浏览器架构与渲染引擎
section: '8.4'
status: 已完成
difficulty: 进阶
lastUpdated: '2026-06-26'
tags:
  - 光栅化
  - GPU
  - Skia
  - VSync
keywords:
  - 光栅化
  - GPU
  - Skia
  - VSync
  - 光栅化与显示
  - 软件光栅化
  - vs
summary: >-
  本文围绕光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync展开，重点说明软件光栅化 vs GPU
  光栅化、Skia、VSync，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
learningGoals:
  - 理解光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync的核心概念、问题背景和适用边界。
  - 掌握光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync在真实前端工程中的典型用法和验证方式。
  - 能够识别光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync相关的常见误区，并选择合适的优化或治理策略。
prerequisites: []
related:
  - title: 8.3 渲染管线深入：解析 HTML、样式计算、布局、绘制、合成
    slug: volume-03/chapter-08/8.3-rendering-pipeline
  - title: 8.5 内存管理：V8 堆分区、标记-清除-整理、增量标记、并发标记
    slug: volume-03/chapter-08/8.5-memory-management
  - title: 8.2 导航流程：URL 输入到页面显示的全过程
    slug: volume-03/chapter-08/8.2-navigation
crossRefs:
  - title: 8.3 渲染管线深入：解析 HTML、样式计算、布局、绘制、合成
    slug: volume-03/chapter-08/8.3-rendering-pipeline
  - title: 8.5 内存管理：V8 堆分区、标记-清除-整理、增量标记、并发标记
    slug: volume-03/chapter-08/8.5-memory-management
references:
  - title: 'Chromium: GPU Accelerated Compositing in Chrome'
    url: >-
      https://www.chromium.org/developers/design-documents/gpu-accelerated-compositing-in-chrome/
  - title: 'web.dev: Rendering performance'
    url: 'https://web.dev/articles/rendering-performance'
  - title: 'Chrome Developers: Inside look at modern web browser, part 3'
    url: 'https://developer.chrome.com/blog/inside-browser-part3'
  - title: Vite Guide
    url: 'https://vite.dev/guide/'
  - title: Webpack Concepts
    url: 'https://webpack.js.org/concepts/'
---
## 速览

- 本文围绕光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync展开，重点说明软件光栅化 vs GPU 光栅化、Skia、VSync，并补充核心机制、代码示例、工程实践、常见误区和参考资料。
- 阅读重点是把「光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync」放回 第 8 章 浏览器架构与渲染引擎 的知识链路中理解，而不是只记忆孤立定义。
- 工程实践中需要同时关注 光栅化、GPU、Skia、VSync 等关键因素，并用可观察的指标或测试结果验证判断。
- 如果你已经熟悉前置概念，可以优先阅读「工作原理」「工程实践」和「常见误区」三部分。

## 背景与问题

光栅化是把绘制指令转换为像素位图。浏览器通常会把页面拆成图层和 tiles，分别光栅化后再由合成器合成。Chrome 底层大量使用 Skia 图形库；具体是否走 GPU 光栅化取决于平台、驱动、内容类型、浏览器策略和回退条件。

在前端工程中，光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync通常不是单独存在的知识点，而是连接语言特性、浏览器能力、框架抽象和工程约束的判断入口。理解它的背景，可以帮助我们回答三个问题：为什么需要这个机制、它解决了什么边界问题，以及在什么场景下不应该过度使用。

## 核心概念

| 概念 | 说明 |
|---|---|
| 光栅化 | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| GPU | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| Skia | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |
| VSync | 需要明确它的定义、适用范围、与相邻概念的区别，以及在代码中的可观察表现。 |

这些概念之间往往存在依赖关系：先理解术语边界，再观察运行机制，最后才能做出稳定的工程决策。

## 工作原理

| 概念 | 说明 |
|---|---|
| Paint | 记录绘制指令 |
| Rasterize | 把绘制指令转换为位图 |
| Tile | 把大图层切成小块，便于按需绘制和缓存 |
| Composite | 把多个图层按 transform、opacity 等组合 |
| VSync | 显示器刷新同步信号，决定帧提交节奏 |

60Hz 屏幕每帧预算约 16.7ms，120Hz 则约 8.3ms。预算不仅给 JS，也包含样式、布局、绘制、光栅化和合成。

| 概念 | 说明 |
|---|---|
| Paint | 记录绘制指令 |
| Rasterize | 把绘制指令转换为位图 |
| Tile | 把大图层切成小块，便于按需绘制和缓存 |
| Composite | 把多个图层按 transform、opacity 等组合 |
| VSync | 显示器刷新同步信号，决定帧提交节奏 |

60Hz 屏幕每帧预算约 16.7ms，120Hz 则约 8.3ms。预算不仅给 JS，也包含样式、布局、绘制、光栅化和合成。

## 代码示例

让滚动和动画更容易走合成路径：

```css
.sheet {
  transform: translateY(100%);
  transition: transform 220ms ease;
}

.sheet[data-open="true"] {
  /* 中文注释：移动已有合成层比反复重排布局更稳定。 */
  transform: translateY(0);
}
```

限制昂贵绘制区域：

```css
.expensive-panel {
  contain: layout paint;
  content-visibility: auto;
  contain-intrinsic-size: 600px;
}
```

用 requestAnimationFrame 对齐帧：

```ts
let pending = false

function scheduleUpdate() {
  if (pending) {
    return
  }

  pending = true
  requestAnimationFrame(() => {
    pending = false
    // 中文注释：在下一帧统一提交视觉更新，减少无意义重复绘制。
    document.body.dataset.frame = String(Date.now())
  })
}
```

## 工程实践

排查掉帧时，优先确认是哪一段超预算：

1. Main thread 忙：JS、样式或布局长任务。
2. Paint 忙：大面积阴影、滤镜、渐变、复杂 SVG。
3. Raster 忙：图片过大、图层过多、tile 反复失效。
4. Composite 忙：层数量过多、纹理内存压力、GPU 回退。

DevTools 的 Rendering、Layers、Performance 面板可以一起使用。不要单看 FPS；要看具体事件和耗时。

在真实项目中落地光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

在真实项目中落地光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync时，可以按以下顺序推进：

| 步骤 | 动作 | 验证方式 |
|---|---|---|
| 识别场景 | 明确它解决的是性能、可维护性、兼容性、安全性还是协作问题。 | 写出触发条件和不适用场景。 |
| 建立基线 | 记录当前实现、指标、错误率或维护成本。 | 使用测试、日志、监控或构建报告作为证据。 |
| 小步改造 | 先在低风险路径验证方案，再扩大到核心链路。 | 对比改造前后的行为和指标。 |
| 固化规范 | 将经验沉淀为 lint、测试、文档或评审清单。 | 新增场景能复用同一套判断标准。 |

## 常见误区

| 常见问题 | 处理建议 |
|---|---|
| 以为 GPU 一定更快 | GPU 有上传、纹理和驱动成本，复杂内容可能回退 |
| 过多合成层 | 控制 `will-change` 和 3D transform 的使用 |
| 大图未压缩 | 使用响应式图片、合适尺寸和现代格式 |
| 复杂滤镜全屏动画 | 缩小影响区域或改为预渲染资源 |
| 只看平均 FPS | 同时看最长帧、掉帧位置和用户操作路径 |

## 对比总结

| 维度 | 关注点 | 判断标准 |
|---|---|---|
| 概念理解 | 光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync的定义、边界和相邻概念。 | 能用自己的话解释，并指出至少一个反例。 |
| 工程使用 | 代码、配置、流程或团队规范中的落地方式。 | 能在真实场景中给出可执行步骤。 |
| 风险控制 | 性能、兼容、安全、可维护性和协作成本。 | 有测试、监控、回滚或审查机制支撑。 |
| 长期维护 | 文档、自动化检查和知识传递。 | 后续成员能按同一标准继续演进。 |

## 自测题

<Quiz
  question={"学习光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync时，最能体现工程化理解的是哪一项？"}
  options={[
    "只记住一个简短定义",
    "能说明适用场景、限制条件、验证方式和常见风险",
    "只复制文章中的代码片段",
    "只关注工具或 API 名称"
  ]}
  correctIndex={1}
  explanation={"工程化理解不止停留在概念定义，还要能说明适用边界、验证方式和风险控制。这样才能把光栅化与显示：软件光栅化 vs GPU 光栅化、Skia、VSync从知识点转化为可落地的工程判断。"}
/>

## 参考资料

- [Chromium: GPU Accelerated Compositing in Chrome](https://www.chromium.org/developers/design-documents/gpu-accelerated-compositing-in-chrome/)
- [web.dev: Rendering performance](https://web.dev/articles/rendering-performance)
- [Chrome Developers: Inside look at modern web browser, part 3](https://developer.chrome.com/blog/inside-browser-part3)
- [Vite Guide](https://vite.dev/guide/)
- [Webpack Concepts](https://webpack.js.org/concepts/)
